---
const { href, text, btnText } = Astro.props
---
<div class="text-gray-400 bg-transparent">
    <div class="container px-5 pb-10 mx-auto">
      <div class="lg:w-full flex flex-col sm:flex-row sm:items-center items-start mx-auto">
        <h1 class="flex-grow sm:pr-16 text-2xl font-medium title-font text-white">{text}</h1>
        <button class="flex-shrink-0 text-white bg-indigo-500 border-0 py-2 px-8 focus:outline-none hover:bg-indigo-600 rounded text-lg mt-10 sm:mt-0">{ btnText }</button>
      </div>
    </div>
</div>